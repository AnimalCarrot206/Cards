local Players = game:GetService("Players")
--!strict
local Class = require(game.ReplicatedStorage.Shared.Class)

local Shoots = Class:extend()

function Shoots:shootWithHit(player: Player, enemy: Player)
    
end

function Shoots:shootWithMiss(player: Player, enemy: Player)
    
end

function Shoots:ambush(players: {Player})
    
end

function Shoots:duel(player: Player, enemy: Player)
    
end

return Shoots